var RV_LESSONS = [{"t":"1. First Light","b":"<p>Every journey into the machine starts with one instruction doing one visible thing. RISC-V programs are lists of instructions the core executes top to bottom. Two things to know right now:</p><ul><li><code>li a0, 42</code> loads the number 42 into register <code>a0</code>. Registers are the core's 32 scratch slots.</li><li><code>ecall</code> asks the machine for a service. Put <code>1</code> in <code>a7</code> first and it prints the integer in <code>a0</code>. Put <code>10</code> in <code>a7</code> and it halts.</li></ul><p>Load the code, hit <strong>Run</strong>, and watch 42 appear in the Console below.</p>","c":"# Your first RISC-V program. It prints 42, then halts.\nli a0, 42      # a0 = 42 (values to print go in a0)\nli a7, 1       # ecall service 1 = print integer in a0\necall\nli a7, 10      # ecall service 10 = halt the machine\necall\n"},{"t":"2. Registers Have Names","b":"<p>The core has 32 registers, <code>x0</code> to <code>x31</code>, each holding one 32-bit value. Assembly gives them human names by convention (the ABI):</p><ul><li><code>zero</code> (x0) is hardwired to 0. Writes to it are silently ignored. It is surprisingly useful.</li><li><code>t0</code>-<code>t6</code> are temporaries, <code>a0</code>-<code>a7</code> are arguments and results, <code>s0</code>-<code>s11</code> are saved values, <code>ra</code> holds return addresses, <code>sp</code> is the stack pointer.</li></ul><p>This program adds with named registers, then prints the result. Try changing the numbers.</p>","c":"li t0, 5        # t0 = 5\nli t1, 10       # t1 = 10\nadd t2, t0, t1  # t2 = t0 + t1 = 15\nmv a0, t2       # mv copies: a0 = t2\nli a7, 1\necall           # print a0\nli a7, 10\necall           # halt\n"},{"t":"3. Arithmetic and Logic","b":"<p>The ALU crew: <code>add</code>, <code>sub</code>, <code>and</code>, <code>or</code>, <code>xor</code>. Append <code>i</code> for the immediate form, which takes a constant instead of a third register (<code>addi t0, t0, 1</code> adds one). Immediates range from -2048 to 2047.</p><p>There is no multiply instruction in base RV32I, by the way. Later you will build one from adds and shifts, like the old masters did.</p>","c":"li t0, 20\nli t1, 6\nadd a0, t0, t1   # 26\nli a7, 1\necall\nsub a0, t0, t1   # 14\necall\nand a0, t0, t1   # 4\necall\nor a0, t0, t1    # 22\necall\nxor a0, t0, t1   # 18\necall\naddi a0, t0, -7  # 13\necall\nli a7, 10\necall\n"},{"t":"4. Decisions","b":"<p>Branches compare two registers and jump to a label when the condition holds: <code>beq</code> (equal), <code>bne</code> (not equal), <code>blt</code> / <code>bge</code> (signed), <code>bltu</code> / <code>bgeu</code> (unsigned). Labels end with a colon. <code>j label</code> jumps unconditionally.</p><p>This is the max-of-two pattern. Everything with an <code>if</code> in it is built from this.</p>","c":"li t0, 17\nli t1, 42\nblt t0, t1, t1_wins  # if t0 < t1, jump\nmv a0, t0             # else a0 = t0\nj done\nt1_wins:\nmv a0, t1             # a0 = t1\ndone:\nli a7, 1\necall                 # prints 42\nli a7, 10\necall\n"},{"t":"5. Loops","b":"<p>A loop is a branch that jumps backward. The pattern: initialize, test, body, step, repeat. This sums 1 through 10. Single-step it and watch <code>t0</code> accumulate in the Registers pane below.</p><p>Every loop needs a way out. Forget the exit branch and the core runs forever, which is exactly what the runaway trap is for. Try deleting the <code>blt</code> line and running.</p>","c":"li t0, 0        # sum = 0\nli t1, 1        # i = 1\nloop:\nadd t0, t0, t1  # sum += i\naddi t1, t1, 1  # i++\nli t2, 11\nblt t1, t2, loop # while i < 11, repeat\nmv a0, t0\nli a7, 1\necall           # prints 55\nli a7, 10\necall\n"},{"t":"6. Memory","b":"<p>Registers are fast and tiny. Memory is bigger and slower. <code>lw</code> loads a word, <code>sw</code> stores one: <code>lw t0, 0(t1)</code> reads the word at address <code>t1 + 0</code>.</p><ul><li>Data lives under a <code>.data</code> directive. Code lives under <code>.text</code>.</li><li><code>la t0, label</code> loads a label's address into a register.</li><li>The stack pointer <code>sp</code> starts at <code>0x1000</code> and grows <em>down</em>: subtract before you store, add after you load.</li></ul>","c":".data\nsecret: .word 0x12345678\n.text\nla t0, secret   # t0 = address of secret\nlw a0, 0(t0)     # a0 = *t0\nli a7, 1\necall            # prints 305419896\n# now the stack: push, then pop\naddi sp, sp, -4\nsw a0, 0(sp)     # push\nlw t1, 0(sp)     # pop into t1\naddi sp, sp, 4\nmv a0, t1\nli a7, 1\necall            # prints it again\nli a7, 10\necall\n"},{"t":"7. Functions","b":"<p><code>jal ra, label</code> jumps to a function and saves the return address in <code>ra</code>. <code>ret</code> jumps back to <code>ra</code>. Arguments go in <code>a0</code>-<code>a7</code>, results come back in <code>a0</code>.</p><p>Nested calls must save <code>ra</code> on the stack first, or the inner call overwrites it and you never get home. This example doubles a number in a function.</p>","c":"li a0, 21\njal ra, double   # call double(21)\nli a7, 1\necall            # prints 42\nli a7, 10\necall\ndouble:\nadd a0, a0, a0   # a0 = a0 * 2\nret              # return to ra\n"},{"t":"8. Bit Wizardry","b":"<p>Shifts move bits: <code>slli</code> (left, fills 0), <code>srli</code> (right logical), <code>srai</code> (right arithmetic, keeps the sign). Shifting left by <code>n</code> multiplies by 2^n. Combine shifts with <code>and</code> masks to pull fields out of packed values.</p><p>This extracts the low byte of a word and divides a negative by 4 the honest way.</p>","c":"li t0, 0xABCD\nandi a0, t0, 0xFF  # low byte = 0xCD = 205\nli a7, 1\necall\nslli t1, t0, 4       # t0 * 16\nsrli a0, t1, 4       # back down (logical)\nli a7, 1\necall\nli t2, -16\nsrai a0, t2, 2       # -16 / 4 = -4, sign kept\nli a7, 1\necall\nli a7, 10\necall\n"},{"t":"9. Big Constants","b":"<p>Immediates only reach -2048 to 2047, but <code>lui</code> loads 20 bits into the <em>upper</em> part of a register, so two instructions build any 32-bit value. You rarely write this by hand: the <code>li</code> pseudoinstruction expands to one or two real instructions automatically.</p><p>Assemble this and check the Machine code pane below: the first <code>li</code> becomes one instruction, the second becomes two.</p>","c":"li t0, 100         # small: one addi\nli t1, 0x12345678  # big: lui + addi\nmv a0, t1\nli a7, 1\necall            # prints 305419896\nli a7, 10\necall\n"},{"t":"10. Talking to the World","b":"<p>The core is deaf and mute except for <code>ecall</code>: service <code>1</code> prints the integer in <code>a0</code>, service <code>4</code> prints the low byte of <code>a0</code> as a character, service <code>10</code> halts. With service 4 and a loop, you can print whole strings.</p><p>This walks a string in memory and prints it character by character. It is a complete, honest program.</p>","c":".data\nmsg: .string \"Hello, silicon!\"\n.text\nla s0, msg\nploop:\nlb t0, 0(s0)        # load one byte\nbeq t0, zero, pdone # stop at the null terminator\nmv a0, t0\nli a7, 4\necall               # print the character\naddi s0, s0, 1\nj ploop\npdone:\nli a7, 10\necall\n"}];
var RV_CHALLENGES = [{"id":"double","t":"Doubler","d":"easy","b":"Read the starting value in <code>a0</code> and leave <code>a0 * 2</code> in <code>a0</code>. Then halt with ecall 10. Tested with 21, 0, and -5.","s":"# a0 holds the input. Leave a0*2 in a0, then halt.\n# e.g. 21 -> 42\n\nli a7, 10\necall\n","h":"add can add a register to itself.","tests":[{"regs":{"10":21},"want":{"regs":{"10":42}}},{"regs":{"10":0},"want":{"regs":{"10":0}}},{"regs":{"10":-5},"want":{"regs":{"10":-10}}}]},{"id":"sumto","t":"Sum 1 to N","d":"easy","b":"<code>a0</code> starts at N. Leave the sum 1 + 2 + ... + N in <code>a0</code>, then halt. Tested with 10 (55), 1 (1), and 100 (5050).","s":"# a0 = N on entry. Leave sum(1..N) in a0, then halt.\n\nli a7, 10\necall\n","h":"Lesson 5 is this exact loop. Accumulate in a temp, count up to N.","tests":[{"regs":{"10":10},"want":{"regs":{"10":55}}},{"regs":{"10":1},"want":{"regs":{"10":1}}},{"regs":{"10":100},"want":{"regs":{"10":5050}}}]},{"id":"max","t":"Maximum","d":"easy","b":"<code>a0</code> and <code>a1</code> hold two signed numbers. Leave the larger in <code>a0</code>, then halt. Tested with (17, 42), (99, 3), and (-5, -2).","s":"# a0, a1 hold two numbers. Leave max(a0, a1) in a0, then halt.\n\nli a7, 10\necall\n","h":"blt with a label: if a0 < a1, copy a1 over a0.","tests":[{"regs":{"10":17,"11":42},"want":{"regs":{"10":42}}},{"regs":{"10":99,"11":3},"want":{"regs":{"10":99}}},{"regs":{"10":-5,"11":-2},"want":{"regs":{"10":-2}}}]},{"id":"fib","t":"Fibonacci","d":"med","b":"<code>a0</code> starts at N (N >= 1). Leave fib(N) in <code>a0</code>, with fib(1) = 1 and fib(2) = 1. Then halt. Tested with 10 (55), 2 (1), and 15 (610).","s":"# a0 = N on entry (N >= 1). Leave fib(N) in a0, then halt.\n# fib(1)=1, fib(2)=1, fib(3)=2, ...\n\nli a7, 10\necall\n","h":"Keep two consecutive values in t0, t1 and roll them forward N-1 times.","tests":[{"regs":{"10":10},"want":{"regs":{"10":55}}},{"regs":{"10":2},"want":{"regs":{"10":1}}},{"regs":{"10":15},"want":{"regs":{"10":610}}}]},{"id":"popcnt","t":"Count the Ones","d":"med","b":"<code>a0</code> holds a 32-bit value. Leave its population count (number of 1 bits) in <code>a0</code>, then halt. Tested with 45 (4), 0 (0), and all-bits-set (32).","s":"# a0 holds a value. Leave its popcount in a0, then halt.\n\nli a7, 10\necall\n","h":"andi with 1 grabs the low bit. srli shifts the next bit into place.","tests":[{"regs":{"10":45},"want":{"regs":{"10":4}}},{"regs":{"10":0},"want":{"regs":{"10":0}}},{"regs":{"10":-1},"want":{"regs":{"10":32}}}]},{"id":"fact","t":"Factorial","d":"med","b":"<code>a0</code> starts at N. Leave N! in <code>a0</code>, then halt. There is no multiply instruction: build one from adds, or add repeatedly. Tested with 6 (720), 1 (1), and 0 (1).","s":"# a0 = N on entry. Leave N! in a0, then halt.\n# No mul instruction: earn it with a loop.\n\nli a7, 10\necall\n","h":"Inner loop: add t1 to an accumulator t2 exactly t3 times. Outer loop counts N down.","tests":[{"regs":{"10":6},"want":{"regs":{"10":720}}},{"regs":{"10":1},"want":{"regs":{"10":1}}},{"regs":{"10":0},"want":{"regs":{"10":1}}}]},{"id":"memcpy","t":"Memory Mover","d":"med","b":"Copy 4 words from address <code>0x800</code> to address <code>0x810</code>, then halt. Use <code>lw</code> and <code>sw</code> with offsets. The test fills the source with secret values.","s":"# Copy 4 words: 0x800 -> 0x810, 0x804 -> 0x814, ...\n# then halt.\n\nli a7, 10\necall\n","h":"li t0, 0x800 gives you the source pointer. lw t1, 0(t0) then sw t1, 16(t0) moves the first word.","tests":[{"mem":{"0x800":[11,22,33,44]},"want":{"mem":{"0x810":[11,22,33,44]}}},{"mem":{"0x800":[-1,0,2147483647,-2147483648]},"want":{"mem":{"0x810":[-1,0,2147483647,-2147483648]}}}]},{"id":"hi","t":"Say HI","d":"easy","b":"Print the characters <code>H</code> then <code>I</code> using ecall service 4 (prints the low byte of <code>a0</code> as a character), then halt with ecall 10. The grader reads the console output.","s":"# Print 'H' then 'I' with ecall service 4, then halt.\n# 'H' is 72, 'I' is 73.\n\nli a7, 10\necall\n","h":"li a0, 72 / li a7, 4 / ecall prints H. Do it twice, then halt.","tests":[{"want":{"out":"HI"}}]}];
var RV_ISAREF = [["add rd,rs1,rs2","rd = rs1 + rs2"],["sub rd,rs1,rs2","rd = rs1 - rs2"],["and / or / xor rd,rs1,rs2","bitwise ops"],["sll / srl / sra rd,rs1,rs2","shift by register (low 5 bits)"],["slt / sltu rd,rs1,rs2","rd = 1 if rs1 < rs2 (signed / unsigned)"],["addi / slti / ... rd,rs1,imm","immediate form, imm -2048..2047"],["slli / srli / srai rd,rs1,sh","shift by constant 0..31"],["lui rd,imm20","rd = imm20 << 12"],["auipc rd,imm20","rd = pc + (imm20 << 12)"],["lw / lh / lb rd,off(rs1)","loads, sign-extended"],["lhu / lbu rd,off(rs1)","loads, zero-extended"],["sw / sh / sb rs2,off(rs1)","stores"],["beq / bne rs1,rs2,label","branch if equal / not equal"],["blt / bge rs1,rs2,label","signed compare branches"],["bltu / bgeu rs1,rs2,label","unsigned compare branches"],["jal rd,label","rd = pc+4, jump to label"],["jalr rd,off(rs1)","rd = pc+4, jump to (rs1+off) with low bit cleared"],["li rd,value","pseudo: any 32-bit constant"],["la rd,label","pseudo: address of label"],["mv rd,rs / j label / ret / nop","common pseudos"],["ecall","a7=1 print int in a0, a7=4 print char in a0, a7=10 halt"],[".text / .data","code segment / data segment (data base 0x800)"],[".word v, ...","32-bit data words"],[".string \"...\"","bytes plus a null terminator"],[".zero n","n zero bytes"]];
